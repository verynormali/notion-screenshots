{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Copy Raw GitHub URL",
      "type": "process",
      "command": "${workspaceFolder}/copy raw automation/copy_raw_url.sh",
      "args": [
        "${file}"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "silent",
        "panel": "dedicated",
        "clear": false
      },
      "problemMatcher": []
    },
    {
      "label": "Copy Raw GitHub URL (Markdown)",
      "type": "process",
      "command": "${workspaceFolder}/copy raw automation/copy_raw_url.sh",
      "args": [
        "--markdown",
        "${file}"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "presentation": {
        "reveal": "silent",
        "panel": "dedicated",
        "clear": false
      },
      "problemMatcher": []
    }
  ]
}